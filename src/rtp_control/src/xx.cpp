#include <iostream>
#include <chrono>

int main() {
    // Get the current time point using high_resolution_clock
    double result=2000000;

    uint64_t convert=static_cast<uint64_t>(result);

    ;
    std::cout << convert << std::endl;
   

    return 0;
}
